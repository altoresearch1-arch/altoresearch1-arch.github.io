# Dependencias del extractor (robot diario en GitHub Actions).
# Solo lo que usan los scripts del robot: fetch_precios, fetch_historicos,
# fetch_hechos, div_stockanalysis, fetch_beneficios, fetch_anual_eps, fix_eps.
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pypdf>=4.2
openpyxl>=3.1
# fetch_gerencia: no todas las empresas presentan su "Análisis y Discusión" en
# PDF — al 01-ago-2026, 10 de 90 lo subieron en Word/Excel (antiguo y moderno).
olefile>=0.46
xlrd>=2.0
