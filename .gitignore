# === Dependencias / build ===
node_modules/
app/node_modules/
app/dist/
dist/

# === Config local de la máquina (permisos de Claude, no compartir) ===
.claude/settings.local.json

# === Python (extractor) ===
# Los PDF de los acuerdos de dividendos se quedan en la máquina (9 MB y subiendo):
# al repo solo viaja leidos.json, que es lo que el robot necesita para no
# re-descargarlos en cada corrida.
extractor/cache_dividendos/*.pdf
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# === Secretos / entorno (NUNCA subir) ===
.env
.env.*
*.key
*.pem
*.pfx
secrets*.json
credenciales*.json

# === Basura del SO / editores ===
.DS_Store
Thumbs.db
*.local
*.log
